CREATE TABLE users (
id          int,
lastname    varchar(64),
firstname   varchar(64),
birthday    date,
email       varchar(64),
phone       varchar(13),
gender      ENUM( "male", "female", "other" ),
timezone    varchar(30),
registration datetime,
ipaddress  varchar(15)
);
